# Optional Unsloth / Qwen3.5 acceleration stack.
# Install PyTorch first from the matching CUDA wheel index, for example:
#   python -m pip install torch==2.7.1+cu128 torchvision==0.22.1+cu128 --index-url https://download.pytorch.org/whl/cu128
#
# Tested on Windows, Python 3.12, RTX 4070 SUPER:
# - CUDA Toolkit 12.8
# - MSVC 2022 x64 build environment for causal-conv1d source builds
#
# Notes:
# - causal-conv1d may need a local Windows source build. The patched local
#   source used for this workspace is under build_wheels/causal_conv1d-1.6.2.post1.
# - If causal-conv1d cannot be installed, Qwen3.5/FLA may fall back to torch kernels.

accelerate==1.13.0
bitsandbytes==0.49.2
cut-cross-entropy==25.1.1
datasets==4.3.0
einops==0.8.2
fla-core==0.5.0
flash-linear-attention==0.5.0
ninja==1.13.0
peft==0.19.1
safetensors==0.8.0rc1
sentencepiece==0.2.1
torchao==0.15.0
transformers==5.5.0
triton-windows==3.3.1.post21
trl==0.24.0
unsloth==2026.5.9
unsloth_zoo==2026.5.5
xformers==0.0.31.post1

# Install separately after CUDA Toolkit/MSVC are available, or from the patched
# local source in this repo:
#   python -m pip install .\build_wheels\causal_conv1d-1.6.2.post1 --no-build-isolation --no-deps
# causal-conv1d==1.6.2.post1
